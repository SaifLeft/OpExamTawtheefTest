<style>
    /* بطاقات مجموعة الأسئلة */
    .question-set-card {
        transition: all 0.3s ease;
        border-radius: 0.5rem;
        box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
    }

    .question-set-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }

    /* أنماط بطاقة الفهرس */
    .index-card-header {
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        padding-bottom: 1rem;
    }

    /* أنماط حالة مجموعة الأسئلة */
    .status-badge {
        font-weight: 600;
        padding: 0.4rem 0.7rem;
    }

    /* أنماط مجموعات الأسئلة في الانتظار */
    .processing-container {
        padding: 2rem;
        text-align: center;
    }

    .processing-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
        display: block;
    }

    /* أنماط قسم الأسئلة */
    .questions-section {
        margin-top: 2rem;
    }

    .question-item {
        border-left: 3px solid #696cff;
        padding-left: 1rem;
        margin-bottom: 1.5rem;
    }

    .question-text {
        font-weight: 500;
    }

    .question-options {
        margin-top: 0.5rem;
        padding-left: 1.5rem;
    }

    .correct-option {
        color: #71dd37;
        font-weight: 600;
    }

    /* أنماط زر الإجراءات */
    .action-button {
        margin-right: 0.5rem;
        margin-bottom: 0.5rem;
    }

    /* أنماط القسم المخصص في صفحة التفاصيل */
    .details-section {
        margin-bottom: 2rem;
        padding: 1.5rem;
        border-radius: 0.5rem;
        background-color: #f5f5f9;
    }

    .details-title {
        margin-bottom: 1rem;
        font-weight: 600;
        color: #566a7f;
    }

    /* أنماط لوحدة الوصف */
    .description-container {
        max-height: 200px;
        overflow-y: auto;
        padding: 1rem;
        background-color: #fff;
        border-radius: 0.375rem;
        border: 1px solid #d9dee3;
    }

    /* أنماط الأقسام في النموذج */
    .form-section {
        margin-top: 2rem;
        padding-top: 2rem;
        border-top: 1px solid #d9dee3;
    }

    .form-section-title {
        margin-bottom: 1.5rem;
        font-weight: 600;
        color: #566a7f;
    }
</style>
