<style>
    /* أنماط مجموعات الأسئلة */
    .question-set-card .status-badge {
        font-size: 0.8rem;
        padding: 0.35rem 0.65rem;
    }

    .question-set-card .card-header {
        background-color: #f8f9fa;
        padding: 0.75rem 1.25rem;
    }

    .question-set-row:hover {
        background-color: rgba(0, 0, 0, 0.03);
        transition: background-color 0.2s ease;
    }

    .dropdown-item-form {
        padding: 0;
        margin: 0;
    }

    .dropdown-item-form button.dropdown-item {
        text-align: right;
        width: 100%;
        background: none;
        border: none;
        padding: 0.5rem 1rem;
    }

    .dropdown-item-form button.dropdown-item:hover {
        background-color: rgba(0, 0, 0, 0.05);
    }

    /* أنماط الأسئلة */
    .accordion-button {
        padding: 0.75rem 1rem;
    }

    .accordion-button:not(.collapsed) {
        background-color: rgba(0, 113, 188, 0.05);
    }

    .accordion-body {
        padding: 1.25rem;
    }

    .list-group-item.list-group-item-success {
        background-color: rgba(40, 167, 69, 0.1);
    }

    /* أنماط البطاقات المتداخلة */
    .card .card {
        margin-bottom: 1rem;
        box-shadow: none;
        border: 1px solid rgba(0, 0, 0, 0.125);
    }

    .card .card .card-header {
        padding: 0.75rem 1rem;
    }
</style>
